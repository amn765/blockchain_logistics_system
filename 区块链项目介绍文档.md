









<center>
  <font face="黑体" size = 150 style="font-weight: bold;">
  	区块链技术应用实践
  </font>
   <br> 
   <br> 
  <center><font face="黑体" size = 150 style="font-weight: bold;">
    ——供应链追踪平台
  </font>
       <br> 
   <br> 
  <center><font face="黑体" size = 150 style="font-weight: bold;">
    项目介绍文档
  </font>
       <br> 
       <br> 
   <br> 
   <br> 
       <br> 
  <center><font face="黑体" size = 5>
    组长：刘安民
  </font>
       <br> 
  <center><font face="黑体" size = 5>
    组员：杨兴涛、吴浩
  </font>
</center> 

<div STYLE="page-break-after: always;"></div>

# 1 项目背景

## 1.1 项目核心愿景

在现代供应链管理中，物流信息不透明、资金流转复杂、数据篡改和人为错误频发，导致企业运营效率低下、信任成本高昂。本项目旨在构建一个基于区块链技术的供应链追踪平台，实现物流、资金及溯源数据的全链路管理。平台不仅提供交易和物流信息的实时监控，更通过区块链的不可篡改和可追溯特性，建立企业之间、企业与金融机构之间的信任网络。通过引入多方节点（供应商、运输商、金融机构和监管方）共同参与维护，形成权责清晰、协作高效、数据可信的供应链生态，彻底解决供应链管理中信任与透明度不足的问题。



## 1.2 物流信息真实性

传统供应链信息多依赖手工记录或中心化系统，容易出现造假、延迟或信息丢失。本平台利用区块链为每一笔物流操作生成不可篡改的上链记录，所有运输、入库、出库操作都有时间戳和节点签名，确保每条信息都可追溯、可验证。供应商、物流公司和监管机构可以实时获取真实可靠的物流状态，从技术上杜绝虚假运输、篡改记录和失信行为，保障企业与客户的数据安全和信任。



## 1.3 资金流转便捷性

供应链中的资金流转涉及多方支付结算，传统模式存在流程复杂、确认延迟、手工差错多等问题。平台通过链上智能合约和 API 对接，实现资金流转自动化和实时化：  
- 支持交易发起、状态追踪、确认收款全流程管理  
- 用户可以随时发起支付并实时查看交易状态  
- 系统自动更新交易记录，减少人工干预和潜在风险  
- 支持历史交易查询和审计，方便企业进行财务管理  

这种模式显著提升了资金流转效率和透明度，为供应链各方提供了可信、高效的结算环境。



## 1.4 去中心化治理可靠性

为了规避传统供应链管理系统的中心化风险（如单点故障、数据篡改或操作失误），平台采用联盟链的去中心化治理模式。核心业务规则通过智能合约固化，由多方节点共同维护，保证操作不可随意更改。联盟链的许可制要求所有参与者完成实名认证，使每一次操作都与现实身份对应，实现全程可追溯。这不仅提高了平台操作的可靠性，也满足企业对合规、审计和信用的严格要求。资金流和物流信息在链上同步记录，进一步确保系统的透明性、可信度和稳定性。



# 2 技术开发方式

这是一个引入了区块链层的 BS 架构项目，总体技术栈如下

## 2.1 技术栈

- **前端**：React、TypeScript、Material UI、Axios
- **后端**：Node.js、Express
- **数据库**：MongoDB
- **区块链网络**：Hyperledger Fabric（可填充实际链码平台）
- **工具**：Git、GitHub、VS Code



## 2.2 前端

前端的功能是提供可视化界面用于展示资金流转、物流追踪和溯源查询等功能，而区块链相关操作及业务逻辑均在后端完成，前端通过调用后端提供的 API 接口实现具体功能。前端框架选择了 **React**，这是一个成熟的前端组件化库，支持响应式数据绑定、虚拟 DOM 渲染和组件化开发模式。语言选择了 **TypeScript**（TS），它是 JavaScript 的超集，提供静态类型检查与更严格的语法约束，能够有效提升代码的可维护性与可读性。

组件库选择了 **Material UI (MUI)**，这是一个基于 React 的高质量 UI 组件库，提供丰富的界面元素和主题定制能力。前端网络请求通过 **Axios** 实现，封装了与后端接口的通信逻辑，包括交易列表获取、交易创建及确认操作等。前端构建工具选择了 **Vite**，包管理器选择了 **npm**。

整个前端系统结构与常规的浏览器-服务器（BS）应用一致，通过页面组件化和状态管理，实现了灵活、可扩展、易维护的可视化界面。



### 2.2.1 API 接口

前端所有的 API 接口都在 `src/api/http.ts` 中定义，HTTP 客户端使用 Axios，后端基地址可以选择 `http://localhost:3001/api`（用于本地开发）或者你的 IP 地址 `http://<你的IP>:3001/api`（用于局域网内多机调试，需要将 `API_BASE_URL` 从 `"http://localhost:3001/api"` 变为 `http://<你的IP>:3001/api`）。为了便于区分不同模块的接口，每个后端模块都单独定义了一个 API 对象。

主要 API 功能如下：

- **用户认证**（`auth`）
  - **登录**：`POST /api/auth/login`
    - 输入邮箱和密码，返回用户信息及 JWT Token，并保存到 `localStorage`。
  - **注册**：`POST /api/auth/register`
    - 提交公司信息和账号信息，完成注册并返回 Token。
- **仪表盘**（`dashboard`）
  - **获取汇总数据**：`GET /api/dashboard/summary`
    - 返回公司产品、交易数量等统计信息。
  - **获取最近交易记录**：`GET /api/dashboard/transactions`
    - 返回最近 15 条交易数据，包括交易金额、类型、交易双方、状态和时间。
- **产品管理**（`products`）
  - **创建商品批次**：`POST /api/products`
    - 提交 SKU、批次号、数量、生产日期及可选元数据，创建新批次。
  - **文件上传**：`POST /api/products/upload-file`
    - 上传质检报告或其他文件。
  - **获取所有产品**：`GET /api/products`
    - 返回产品列表及基础信息。
- **财务交易**（`finance`）
  - **获取交易列表**：`GET /api/finance/transactions`
    - 返回交易数据，并映射到前端需要的字段，如 `id`、`amount`、`currency`、`status`、`payer`、`payee` 等。
  - **确认收款**：`POST /api/finance/transactions/:id/confirm`
    - 将指定交易状态更新为 `CONFIRMED`。
  - **创建交易**：`POST /api/transactions`
    - 发起支付交易，输入交易金额、收款方、备注等信息。
  - **获取交易详情**：`GET /api/finance/transactions/:id`
    - 获取单笔交易的详细进度，包括发起、验证、上链和确认的各步骤状态。
- **物流管理**（`logistics` / `logisticsEvents`）
  - **获取物流列表**：`GET /api/products`
    - 返回所有产品的物流状态及当前位置。
  - **获取物流详情**：`GET /api/trace/search?query=<productId>`
    - 返回单个产品的物流历史记录。
  - **添加物流事件**：`POST /api/logistics`
    - 提交物流事件信息，包括类型、位置、操作人、时间和备注。
  - **获取物流事件历史**：`GET /api/trace/search?query=<productId>`
    - 返回产品的全部物流事件历史。
- **溯源查询**（`trace`）
  - **查询产品全链路信息**：`GET /api/trace/search?query=<关键词>`
    - 返回产品信息、物流事件列表以及相关交易记录。
- **用户信息**（`users`）
  - **获取当前用户信息**：`GET /api/users/current`
    - 返回当前登录用户的基础信息，包括公司名、邮箱、角色、创建时间等。



### 2.2.2 组件

Vue 组件都放在 `src/components` 中，整体遵循“模块化、可复用”的设计思路。
主要组件类型包括：

- **导航与布局相关**
  - `Sidebar`：提供全局导航和侧边栏菜单，支持路由跳转、高亮当前页等功能。
  - `Layout`：整体页面框架，包含头部导航栏、侧边栏和内容区域，统一处理响应式布局与滚动。
- **业务功能组件**
  - `AddBatchForm`、`AddLogisticsEventForm`、`CreatePaymentForm` 等对话框组件：用于数据创建与表单提交，支持表单校验、异步请求、状态反馈（loading / error / success）。
  - 表单组件通常封装了 Ant Design Vue 的 `Form`、`Input`、`Select`、`Button` 等基础控件，并增加自定义逻辑，如文件上传、时间格式化、动态下拉选项等。
- **辅助组件**
  - 弹窗组件：对话框形式展示表单，提供确认与取消操作。



### 2.2.3 路由

路由模块用于管理系统的页面导航逻辑，基于 **React Router v6** 实现。

在 `src/router/index.tsx` 中定义了各功能页面的访问路径及层级结构。登录后用户将进入 `/app` 主应用区块，其中包含多个功能子页面，如仪表盘（Dashboard）、物流追踪（Logistics）、资金流转（Finance）、用户管理（Users）和溯源查询（Trace）等。

路由配置采用嵌套路由结构，外层为带侧边栏和导航栏的 `Layout` 组件，用于统一页面布局；内层定义了各功能模块页面的访问路径。



### 2.2.4 页面

所有页面组件均放置在 `src/pages` 目录下，每个文件对应一个独立的业务模块页面，例如 `Login.tsx`、`Dashboard.tsx`、`Finance.tsx`、`Logistics.tsx`、`Users.tsx` 和 `Trace.tsx` 等。
这些页面名称与路由路径基本保持一致，便于维护与快速定位。

通用的**布局组件**（如导航栏、侧边栏、主体框架）集中放在 `src/components` 文件夹下，其中 `Layout` 组件作为应用的统一容器，为各业务页面提供一致的结构与导航。



## 2.3 后端

后端采用 **Node.js** 和 **Express** 框架构建 RESTful API，作为连接前端与数据库、区块链网络的桥梁。后端负责处理业务逻辑、数据持久化以及与链码的交互。

- **核心框架与中间件**
    - **Express**：作为核心 Web 框架，用于构建路由和 API。
    - **Mongoose**：用于连接 **MongoDB** 数据库，通过 Schema 定义数据模型。
    - **CORS**：处理跨域资源共享。
    - **Helmet**：通过设置 HTTP 头增加应用安全性。
    - **express-rate-limit**：实现 API 请求速率限制，防止恶意攻击。
    - **Multer**：用于处理文件上传（如质检报告）。
    - **DotEnv**：管理环境变量和敏感配置（如数据库连接字符串和 JWT 密钥）。

- **认证与授权**
    - **JWT (jsonwebtoken)**：通过 `POST /api/auth/login` 签发 Token。
    - **bcryptjs**：在用户注册 (`POST /api/auth/register`) 时对密码进行哈希加密存储，并在登录时进行比对。
    - **Auth 中间件** (`backend/middleware/auth.js`)：一个自定义中间件，用于保护需要授权的路由。它会验证请求头中的 `Bearer Token`，解析用户信息并附加到 `req.user` 对象。

- **数据模型 (MongoDB)**
    - **User** (`models/User.js`)：存储企业用户信息，包括公司名、联系人、邮箱、哈希密码、营业执照号和企业类型。
    - **Product** (`models/Product.js`)：存储产品（批次）信息，包含区块链ID (`id`)、SKU、批号、数量、所有者、当前状态（如 'pending', 'shipped', 'in_transit'）、当前位置以及质检报告ID等。
    - **Transaction** (`models/Transaction.js`)：存储资金流转记录，包含链上ID (`id`)、关联产品ID、付款方、收款方、金额、状态（PENDING, CONFIRMED, FAILED）和时间戳。
    - **LogisticsEvent** (`models/LogisticsEvent.js`)：存储物流事件，包含链上ID (`id`)、产品ID、位置、状态（如 `received`, `shipped`）、经办人和时间戳。

- **Express 路由 (API)**
    - `auth.js` (`/api/auth`)：处理用户注册和登录。
    - `user.js` (`/api/users`)：提供 `GET /current` 接口获取当前登录用户信息。
    - `product.js` (`/api/products`)：实现产品批次的创建 (`POST /`)、查询 (`GET /`) 和文件上传 (`POST /upload-file`)。
    - `logistics.js` (`/api/logistics`)：实现物流事件的添加 (`POST /`)。
    - `transaction.js` (`/api/transactions`)：实现金融交易的创建 (`POST /`) 和查询 (`GET /`)。
    - `finance.js` (`/api/finance`)：提供额外的财务接口，如获取交易列表 (`GET /transactions`) 和确认收款 (`POST /transactions/:id/confirm`)。
    - `dashboard.js` (`/api/dashboard`)：提供仪表盘数据，包括汇总统计 (`GET /summary`) 和最近交易 (`GET /transactions`)。
    - `trace.js` (`/api/trace`)：提供核心溯源接口 (`GET /search`)，用于联合查询产品、物流和交易信息。

- **区块链交互**
    - 后端通过 `fabric-network` 和 `fabric-ca-client` 库与 Hyperledger Fabric 网络通信
    - 在创建产品、添加物流、创建交易时，会调用 `contract.submitTransaction(...)` 将数据提交到链上（如调用链码的 `CreateProduct`, `UpdateLogisticsRecord`, `CreateTransaction` 函数），同时将数据写入 MongoDB 以便快速查询。


## 2.4 区块链网络

- 区块链网络采用 Hyperledger Fabric（或其他企业级链码平台）
- 交易上链，保证数据不可篡改
- 支持交易状态追踪及验证
- 详细链码逻辑待填充



# 3 团队组成和分工

## 3.1 团队组成

小组内共三名成员：

- 组长：刘安民
- 组员：杨兴涛、吴浩

## 3.2 分工

- 刘安民：
- 杨兴涛：
  - 负责后端 Node.js (Express) 服务器的搭建和维护。
  - 设计和实现 MongoDB 数据库的 Schema（包括 User, Product, LogisticsEvent, Transaction）。
  - 开发所有核心业务逻辑的 RESTful API 接口，包括用户认证、产品管理、物流追踪、资金流转和仪表盘数据。
  - 实现基于 JWT (jsonwebtoken) 和 bcryptjs 的用户注册、登录及密码加密功能。
  - 编写 Express 中间件（如 `auth.js`）实现 API 路由的权限验证。
  - 集成 Multer 实现质检报告等文件的上传功能。
  - 编写与 Hyperledger Fabric 链码的交互逻辑，实现关键数据的上链和数据库同步。
- 吴浩：



# 4 最终成果

## 4.1 注册登录

在**登录页面**，用户需要填写邮箱和密码，要求如下：

- 邮箱将进行格式校验，必须符合 `xxx@yy.com` 的规范；
- 系统会验证邮箱与密码的匹配关系，密码错误时会在前端给出明确提示；
- 后端会对输入的密码进行哈希比对，不会以明文形式传输或存储；
- 登录成功后，系统会返回一个带有有效期的访问令牌（token），前端将其保存于本地，用于后续身份验证；
- 若用户连续多次输入错误密码，系统会触发简单的防暴力破解机制，暂时限制登录请求；
- 对于拥有管理权限的用户，系统会在登录时进行额外的身份验证与权限校验。



## 4.2 物流追踪

- 通过区块链记录物流信息
- 支持按产品 ID 查询物流状态
- 页面设计与流程待填充

## 4.3 资金流转

- 前端实现交易列表、状态筛选、确认收款
- 支持发起支付
- 后端实现交易记录 CRUD、状态更新
- 支持交易上链和进度追踪

## 4.4 溯源查询

- 根据产品 ID 查询供应链完整记录
- 可追踪原材料、生产、物流信息
- 页面与链码接口待填充

# 5 后期改进思路

### 5.1 实时数据同步与消息队列

目前系统的数据交互主要依赖后端轮询获取或主动请求，在高并发或多节点部署的情况下，可能出现状态延迟更新的问题。后期可引入 **消息队列（如 RabbitMQ、Kafka）** 或 **WebSocket 实时通信机制**，实现物流状态、交易进度的实时同步，减少接口轮询带来的性能开销。

此外，可在区块链交易写入前引入 **Redis 缓存层**，用于暂存交易请求与事件数据，提升系统的瞬时吞吐能力与响应速度。



### 5.2 智能风控与资金验证

当前的资金流转模块仅实现了交易记录与状态确认，后续可引入 **智能风控机制**，通过对交易历史、账户行为和异常金额变动的分析，自动识别可疑操作。

同时，可与区块链上智能合约深度集成，实现链上资金的自动验证与清算，从而保证每笔交易的 **可追溯性、不可抵赖性和自动结算性**。



### 5.3 区块链数据上链优化

目前部分数据（如物流事件、溯源记录）仅在数据库中保存，可进一步扩展为 **关键数据自动上链**。

例如将产品生命周期的关键节点（生产、运输、交付、质检等）通过智能合约记录在区块链上，以保证全程透明可信。

同时可优化上链策略，采用 **分层上链与哈希摘要上链** 的混合模式，在确保安全性的同时降低链上存储成本。



### 5.4 溯源查询与数据可视化增强

现有溯源功能以标签与产品编号查询为主，后续可引入更智能的 **多维度查询** 与 **数据可视化展示**。

例如支持按时间轴展示产品流转路径，结合地图组件动态标注物流轨迹，并通过仪表盘可视化不同地区、环节的业务分布与异常情况。

此外，还可开发 **企业级报表系统**，支持导出月度、季度或年度运营数据，辅助管理决策。



### 5.5 权限管理与企业协同

当前系统默认用户权限较为简单，后续可引入 **基于角色的访问控制（RBAC）** 机制，对供应商、制造商、运输方、监管方等角色赋予差异化权限。

同时，通过建立 **企业协同网络**，可让不同参与方在统一平台上共享可信数据，实现跨企业、跨节点的业务协作。